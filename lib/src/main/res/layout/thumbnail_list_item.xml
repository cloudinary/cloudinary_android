<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="@dimen/thumbnail_size"
    android:layout_height="@dimen/thumbnail_size">

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:cropToPadding="true"
        android:padding="@dimen/thumbnail_border_width" />

    <ImageView
        android:id="@+id/mediaTypeIcon"
        android:layout_width="@dimen/thumbnail_media_type_icon_size"
        android:layout_height="@dimen/thumbnail_media_type_icon_size"
        android:layout_gravity="bottom|left"
        android:layout_marginLeft="@dimen/thumbnail_media_type_icon_left_margin"
        android:layout_marginBottom="@dimen/thumbnail_media_type_icon_bottom_margin"
        android:visibility="gone" />

</FrameLayout>
